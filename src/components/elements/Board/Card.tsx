import React from 'react'
import { TITLE_NAME } from './Board'

const Card = ({ item }: { item: cardtype }) => {
  const { TO_DO, IN_PROGRESS, DONE, NOTE } = TITLE_NAME
  return <div></div>
}

export default Card
